---
import Layout from "@layouts/Layout.astro";
import Container from "@components/Container.astro";
import { HOME, SITE } from "@consts";
---

<Layout title={HOME.TITLE} description={HOME.DESCRIPTION} fullPage={true}>
  <div class="flex w-full flex-col">
    <!-- Hero -->
    <section class="border-b border-border py-20 md:py-28">
      <Container>
        <h1 class="text-3xl font-semibold tracking-tight text-ink md:text-4xl lg:text-[2.75rem] lg:leading-[1.15]">
          Interactive Campaign Infrastructure for Creator-Led Brands.
        </h1>
        <p class="mt-6 max-w-2xl text-lg text-ink/85 leading-relaxed">
          Mobile-first web experiences that turn passive audiences into engagement you can measure. Drops, launches, brand moments.
        </p>
        <p class="mt-4 text-sm font-medium uppercase tracking-wider text-ink-muted">
          Playable systems for modern media
        </p>
        <div class="mt-12 flex items-center gap-4 text-sm text-ink-muted">
          <span>Action</span>
          <span class="text-border">→</span>
          <span>Interaction</span>
          <span class="text-border">→</span>
          <span>Data</span>
          <span class="text-border">→</span>
          <span>Conversion</span>
        </div>
      </Container>
    </section>

    <!-- The Problem -->
    <section class="border-b border-border py-16 md:py-20">
      <Container>
        <h2 class="text-xs font-semibold uppercase tracking-wider text-ink-muted">
          The Problem
        </h2>
        <p class="mt-4 max-w-2xl text-lg text-ink/90 leading-relaxed">
          Most creator campaigns are passive. Scroll, watch, like. Brands can't measure depth of attention. Campaigns look the same. The data never gets used.
        </p>
      </Container>
    </section>

    <!-- The Solution -->
    <section class="border-b border-border py-16 md:py-20">
      <Container>
        <h2 class="text-xs font-semibold uppercase tracking-wider text-ink-muted">
          The Solution
        </h2>
        <p class="mt-4 max-w-2xl text-lg text-ink/90 leading-relaxed">
          Mobile-first experiences, 60 to 120 seconds, QR or link launch. No app store. Analytics included. Same engine runs drops, launches, and brand moments.
        </p>
      </Container>
    </section>

    <!-- How It Works -->
    <section class="border-b border-border py-16 md:py-24">
      <Container>
        <h2 class="text-xs font-semibold uppercase tracking-wider text-ink-muted">
          How It Works
        </h2>
        <div class="mt-12 grid gap-10 sm:grid-cols-2 lg:grid-cols-4">
          <div>
            <p class="text-xs font-semibold text-ink-muted">01</p>
            <p class="mt-2 font-semibold text-ink">Align on Campaign Goal</p>
            <p class="mt-1 text-ink/75 text-[15px]">Lock in the moment, audience, and what success looks like.</p>
          </div>
          <div>
            <p class="text-xs font-semibold text-ink-muted">02</p>
            <p class="mt-2 font-semibold text-ink">Deploy Playable System</p>
            <p class="mt-1 text-ink/75 text-[15px]">We build the experience. Mobile-first, fast.</p>
          </div>
          <div>
            <p class="text-xs font-semibold text-ink-muted">03</p>
            <p class="mt-2 font-semibold text-ink">Launch Across Channels</p>
            <p class="mt-1 text-ink/75 text-[15px]">QR, links, social.</p>
          </div>
          <div>
            <p class="text-xs font-semibold text-ink-muted">04</p>
            <p class="mt-2 font-semibold text-ink">Measure and optimise</p>
            <p class="mt-1 text-ink/75 text-[15px]">See what works.</p>
          </div>
        </div>
      </Container>
    </section>

    <!-- Who It's For -->
    <section class="border-b border-border py-16 md:py-20">
      <Container>
        <h2 class="text-xs font-semibold uppercase tracking-wider text-ink-muted">
          Who It's For
        </h2>
        <ul class="mt-6 max-w-2xl text-lg text-ink/90 leading-relaxed">
          <li class="border-b border-border py-3 first:pt-0">Creator-led studios</li>
          <li class="border-b border-border py-3">Media networks</li>
          <li class="border-b border-border py-3">Brand campaign teams</li>
          <li class="border-b border-border py-3">Digital-first DTC brands</li>
          <li class="border-b border-border py-3 last:border-b-0">Cultural brands launching drops</li>
        </ul>
      </Container>
    </section>

    <!-- Built as Infrastructure -->
    <section class="border-b border-border bg-surface-elevated py-16 md:py-20">
      <Container>
        <h2 class="text-xs font-semibold uppercase tracking-wider text-ink-muted">
          One engine, many campaigns
        </h2>
        <p class="mt-4 max-w-2xl text-lg text-ink/90 leading-relaxed">
          We build it once. You run it across drops, launches, whatever you need. Fast turnaround, configurable, and it fits how you already work.
        </p>
      </Container>
    </section>

    <!-- Decision Labs -->
    <section class="border-b border-border py-16 md:py-20">
      <Container>
        <h2 class="text-xs font-semibold uppercase tracking-wider text-ink-muted">
          Decision Labs
        </h2>
        <p class="mt-4 max-w-2xl text-lg text-ink/90 leading-relaxed">
          Playable simulations before you build. Run concepts and direction through a quick test before a campaign ships.
        </p>
      </Container>
    </section>

    <!-- CTA -->
    <section class="py-20 md:py-24">
      <Container>
        <p class="text-xl font-semibold text-ink">
          Discuss a Playable Campaign.
        </p>
        <p class="mt-3 text-ink-muted">
          <a href={`mailto:${SITE.EMAIL}`} class="underline underline-offset-4 decoration-ink/30 transition-colors hover:decoration-ink">Get in touch</a>
        </p>
      </Container>
    </section>
  </div>
</Layout>
